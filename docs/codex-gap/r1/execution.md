# R1 执行记录

批准范围：P0+B3–B8；生产/main合并另确认；真实短任务总上限24，A/B最多12、候选最多12；登录异常/真实429立即停止支线。

基线已重验：`abe449690bda37ebaf68485535358daec0e99d34`，feature工作区干净；官方v0.2.5仍为基线，没有自动升级。

P0环境：Windows主机的默认Docker named pipe不可达；已从WSL Ubuntu24.04真实验证Linux Docker Unix socket返回200、Docker29.1.3。WSL有gcc/python3/go/node，后续需确认Go版本并用锁定工具链。

GitHub：gh未登录、后台git push --dry-run因隔离凭据不可用而失败。推送/CI支线需要正式用户授权；不能把公开个人资料当写入权限。不读其他进程/用户的凭据。

生产只读检查：当前实际程序仍为官方v0.2.5。参考实例存在，无新模型任务。历史采样不重复计入新R1预算，R1台账从0起逐次预占。

阶段状态：P0进行中；B3准备；B4–B8待验收。测试网络与下载网络分离；不在生产VPS构建/压测。
